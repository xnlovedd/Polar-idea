<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="polar.island.database.SqlDao">
	<select id="querySql" parameterType="string" resultType="java.util.HashMap">
		${sql}
	</select>
	<select id="queryOne" parameterType="string" resultType="java.util.HashMap">
		${sql}
	</select>
	<insert id="insertSql" parameterType="string">
		${sql}
	</insert>
	<update id="updateSql" parameterType="string">
		${sql}
	</update>
</mapper>